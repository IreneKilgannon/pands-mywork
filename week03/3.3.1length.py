# A program that reads in a string and output how long it is.

input_string = input("Please enter a string: ")

print(f"The length of {input_string} is {len(input_string)}")

# Solution slightly different lenght_ofstring = len(input_string), with lenght_of_string in the print